const { oppositeNumber } = require('../src');

describe('oppositeNumber', () => {
  it('should return the opposite of the number entered', () => {
    expect(oppositeNumber(1)).toEqual(-1);

  });


});
